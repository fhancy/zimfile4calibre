[project]
name = "zim-import"
version = "0.1.0"
description = "Import Gutenberg OpenZIM collections into Calibre"
requires-python = ">=3.11"
dependencies = ["zstandard>=0.22"]

[tool.setuptools.packages.find]
include = ["zim_import*"]
