[project]
name = "zim-import"
version = "0.1.0"
description = "Read Gutenberg OpenZIM catalogs and extract EPUB/PDF files"
requires-python = ">=3.11"
dependencies = ["zstandard>=0.22"]

[tool.setuptools.packages.find]
include = ["zim_import*"]
